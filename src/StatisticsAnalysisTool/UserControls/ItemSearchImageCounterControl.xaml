<UserControl x:Class="StatisticsAnalysisTool.UserControls.ItemSearchImageCounterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200" 
             d:DataContext="{d:DesignData ItemSearchImageCounterControl}">
    <StackPanel Orientation="Horizontal">
        <Grid Margin="10,0,0,0" Width="24" Height="24" VerticalAlignment="Center" >
            <fa5:ImageAwesome Icon="Regular_Image" Foreground="{StaticResource SolidColorBrush.Text.1}" />
        </Grid>
        <Label Content="{Binding LocalImageCounter, FallbackValue=0}" Height="24" Margin="0,0,0,2" VerticalAlignment="Center" 
               Foreground="{StaticResource SolidColorBrush.Text.1}" Width="auto" VerticalContentAlignment="Center" />
        <StackPanel Orientation="Horizontal" Height="24" VerticalAlignment="Center" Margin="10,0,10,0" >
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" HorizontalContentAlignment="Center" />
        </StackPanel>
        <Label Content="{Binding ItemCounterString, FallbackValue=0/0}" Height="24" Margin="0,0,10,0" VerticalAlignment="Center" 
               Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalContentAlignment="Center" HorizontalAlignment="Center" Width="auto" />
    </StackPanel>
</UserControl>
