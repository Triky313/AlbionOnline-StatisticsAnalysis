<UserControl x:Class="StatisticsAnalysisTool.UserControls.DamageMeterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:damageMeter="clr-namespace:StatisticsAnalysisTool.DamageMeter"
             xmlns:enumerations="clr-namespace:StatisticsAnalysisTool.Enumerations"
             xmlns:viewModels="clr-namespace:StatisticsAnalysisTool.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1200"
             d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}">
    <UserControl.Resources>
        <DataTemplate x:Key="DamageStatsEntryTemplate" DataType="{x:Type damageMeter:DamageStatsEntry}">
            <Grid Height="22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="58" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat='{}{0}.'}" Style="{StaticResource TopStats.EntryRank}" />
                <TextBlock Grid.Column="1" Text="{Binding PlayerName}" Style="{StaticResource TopStats.EntryName}" />
                <ProgressBar Grid.Column="2" Value="{Binding BarPercentage}" Margin="5,0,7,0" VerticalAlignment="Center"
                             Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                             Style="{StaticResource TopStats.RankingBar}" />
                <TextBlock Grid.Column="3" Text="{Binding ValueString}"
                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                           Style="{StaticResource TopStats.EntryValue}" />
            </Grid>
        </DataTemplate>
        <DataTemplate x:Key="DamageStatsContributionEntryTemplate" DataType="{x:Type damageMeter:DamageStatsEntry}">
            <Grid Height="22">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="80" />
                    <ColumnDefinition Width="58" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat='{}{0}.'}" Style="{StaticResource TopStats.EntryRank}" />
                <TextBlock Grid.Column="1" Text="{Binding PlayerName}" Style="{StaticResource TopStats.EntryName}" />
                <ProgressBar Grid.Column="2" Value="{Binding BarPercentage}" Margin="5,0,7,0" VerticalAlignment="Center"
                             Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                             Style="{StaticResource TopStats.RankingBar}" />
                <TextBlock Grid.Column="3" Text="{Binding SharePercentageString}"
                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                           Style="{StaticResource TopStats.EntryValue}" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="DamageSpellStatsEntryTemplate" DataType="{x:Type damageMeter:DamageSpellStatsEntry}">
            <Grid Height="38" Margin="0,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="42" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="50" />
                    <ColumnDefinition Width="70" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat='{}{0}.'}" Style="{StaticResource TopStats.EntryRank}" Margin="5,0,0,0" />
                <Ellipse Grid.Column="1" Width="40" Height="40" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTipService.ShowDuration="60000">
                    <Ellipse.ToolTip>
                        <ToolTip Style="{StaticResource Spell.ToolTip}"
                                 DataContext="{Binding PlacementTarget.DataContext.SpellInformation, RelativeSource={RelativeSource Self}}">
                            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource Spell.ToolTipContentTemplate}" />
                        </ToolTip>
                    </Ellipse.ToolTip>
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding Icon}" Stretch="UniformToFill" />
                    </Ellipse.Fill>
                </Ellipse>
                <TextBlock Grid.Column="2" Margin="5,0,0,0" Text="{Binding DisplayName}" Style="{StaticResource TopStats.EntryName}" />
                <ProgressBar Grid.Column="3" Value="{Binding BarPercentage}" Margin="5,0,5,0"
                             VerticalAlignment="Center" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" Style="{StaticResource TopStats.RankingBar}" />
                <TextBlock Grid.Column="4" Text="{Binding ValueString}" Style="{StaticResource TopStats.DamageSpellValue}" />
                <TextBlock Grid.Column="5" Text="{Binding SharePercentageString}" Style="{StaticResource TopStats.DamageSpellShare}" Margin="0,0,10,0" />
            </Grid>
        </DataTemplate>

        <DataTemplate x:Key="DamageSpellStatsColumnTemplate">
            <StackPanel Margin="5,5,5,5">
                <ItemsControl ItemsSource="{Binding}" ItemTemplate="{StaticResource DamageSpellStatsEntryTemplate}" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="DamageTypeStatsEntryTemplate" DataType="{x:Type damageMeter:DamageTypeStatsEntry}">
            <Border Margin="3" Padding="7,6" MinHeight="118"
                    Background="{StaticResource SolidColorBrush.Background.3}"
                    BorderBrush="{StaticResource SolidColorBrush.Border.2}"
                    BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="28" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <iconPacks:PackIconGameIcons x:Name="DamageTypeIcon" Grid.Row="0" Width="34" Height="34" Margin="0,1,0,5" HorizontalAlignment="Center" />
                    <TextBlock Grid.Row="1" Text="{Binding DisplayName}" Foreground="{StaticResource SolidColorBrush.Text.2}" FontSize="11"
                               TextAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Center" />
                    <TextBlock Grid.Row="2" Text="{Binding ValueString}"
                               Foreground="{StaticResource SolidColorBrush.Text.1}" FontSize="14" FontWeight="SemiBold"
                               HorizontalAlignment="Center" />
                    <TextBlock Grid.Row="3" Text="{Binding SharePercentageString}"
                               Foreground="{StaticResource SolidColorBrush.Text.2}" FontSize="11"
                               HorizontalAlignment="Center" />
                    <ProgressBar x:Name="DamageTypeBar" Grid.Row="4" Value="{Binding BarPercentage}"
                                 Margin="2,7,2,0"
                                 Style="{StaticResource TopStats.RankingBar}" />
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding DamageType}" Value="{x:Static enumerations:DamageType.Physical}">
                    <Setter TargetName="DamageTypeIcon" Property="Kind" Value="Broadsword" />
                    <Setter TargetName="DamageTypeIcon" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Red.2}" />
                    <Setter TargetName="DamageTypeBar" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Red.2}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding DamageType}" Value="{x:Static enumerations:DamageType.Magic}">
                    <Setter TargetName="DamageTypeIcon" Property="Kind" Value="CrystalWand" />
                    <Setter TargetName="DamageTypeIcon" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                    <Setter TargetName="DamageTypeBar" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                </DataTrigger>
                <DataTrigger Binding="{Binding DamageType}" Value="{x:Static enumerations:DamageType.True}">
                    <Setter TargetName="DamageTypeIcon" Property="Kind" Value="ShieldDisabled" />
                    <Setter TargetName="DamageTypeIcon" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Orange.3}" />
                    <Setter TargetName="DamageTypeBar" Property="Foreground" Value="{StaticResource SolidColorBrush.Accent.Orange.3}" />
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
        <DataTemplate x:Key="YourStatsTopEntryTemplate" DataType="{x:Type damageMeter:DamageMeterYourStatsTopEntry}">
            <Border Style="{StaticResource YourStats.Defense.TopEntryCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="90" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat='{}{0}.'}" Style="{StaticResource YourStats.TopEntryRank}" />
                    <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource YourStats.TopEntryName}" />
                    <TextBlock Grid.Column="2" Text="{Binding ValueString}" Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" Style="{StaticResource YourStats.TopEntryValue}" />
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="YourStatsHealingTopEntryTemplate" DataType="{x:Type damageMeter:DamageMeterYourStatsTopEntry}">
            <Border Style="{StaticResource YourStats.Healing.TopEntryCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="90" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding Rank, StringFormat='{}{0}.'}" Style="{StaticResource YourStats.TopEntryRank}" />
                    <TextBlock Grid.Column="1" Text="{Binding Name}" Style="{StaticResource YourStats.TopEntryName}" />
                    <TextBlock Grid.Column="2" Text="{Binding ValueString}" Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" Style="{StaticResource YourStats.TopEntryValue}" />
                </Grid>
            </Border>
        </DataTemplate>
        <DataTemplate x:Key="YourStatsSnapshotTemplate" DataType="{x:Type damageMeter:DamageMeterYourStatsSnapshot}">
            <ScrollViewer Margin="0,7,0,0"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalContentAlignment="Stretch">
                <Grid Margin="3,0,3,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Grid.Column="0" Margin="0,2,5,5"
                            Style="{StaticResource YourStats.Damage.SectionCard}">
                        <StackPanel>
                            <Grid Margin="2,0,2,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource YourStats.Damage.HeaderIcon}">
                                    <fa5:ImageAwesome Icon="Solid_FistRaised" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                </Border>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DataContext.DamageMeterBindings.TranslationYourStatsDamageSection, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=YOUR_STATS_DAMAGE_SECTION}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                           Style="{StaticResource YourStats.SectionTitle}" />
                            </Grid>
                            <UniformGrid Columns="3">
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTotalDamage, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL_DAMAGE}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding TotalDamage}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTotalDps, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL_DPS}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding TotalDps}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPeakDpsFiveSeconds, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PEAK_DPS_5_SECONDS}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding PeakDpsFiveSeconds}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            <UniformGrid Columns="5">
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPeakDpsThreeSeconds, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PEAK_DPS_3_SECONDS}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding PeakDpsThreeSeconds}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPeakDpsTenSeconds, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PEAK_DPS_10_SECONDS}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding PeakDpsTenSeconds}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationBiggestHit, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BIGGEST_HIT}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding BiggestHit}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPveDamage, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PVE_DAMAGE}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding PveDamage}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Damage.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPvpDamage, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PVP_DAMAGE}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding PvpDamage}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="0" Grid.Column="1" Margin="5,2,0,5"
                            Style="{StaticResource YourStats.Healing.SectionCard}">
                        <StackPanel>
                            <Grid Margin="2,0,2,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource YourStats.Healing.HeaderIcon}">
                                    <fa5:ImageAwesome Icon="Regular_Heart" Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                                </Border>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DataContext.DamageMeterBindings.TranslationYourStatsHealingSection, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=YOUR_STATS_HEALING_SECTION}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Green.3}"
                                           Style="{StaticResource YourStats.SectionTitle}" />
                            </Grid>
                            <UniformGrid Columns="3">
                                <Border Style="{StaticResource YourStats.Healing.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTotalHealing, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL_HEALING}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding TotalHealing}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Healing.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationEffectiveHealing, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=EFFECTIVE_HEALING}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding EffectiveHealing}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Healing.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationOverhealPercent, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=OVERHEAL_PERCENT}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding OverhealPercent}" Foreground="{StaticResource SolidColorBrush.Accent.Green.3}"
                                                   Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationHealingTargetsTopThree, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=HEALING_TARGETS_TOP_3}"
                                       Foreground="{StaticResource SolidColorBrush.Accent.Green.1}"
                                       FontSize="12" FontWeight="Bold" Margin="2,8,2,3" />
                            <ItemsControl ItemsSource="{Binding TopHealingTargets}" ItemTemplate="{StaticResource YourStatsHealingTopEntryTemplate}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="0" Margin="0,5,5,2"
                            Style="{StaticResource YourStats.Defense.SectionCard}">
                        <StackPanel>
                            <Grid Margin="2,0,2,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource YourStats.Defense.HeaderIcon}">
                                    <fa5:ImageAwesome Icon="Solid_ShieldAlt" Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                                </Border>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DataContext.DamageMeterBindings.TranslationYourStatsDefenseSection, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=YOUR_STATS_DEFENSE_SECTION}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"
                                           Style="{StaticResource YourStats.SectionTitle}" />
                            </Grid>
                            <UniformGrid Columns="2">
                                <Border Style="{StaticResource YourStats.Defense.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationDamageTaken, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DAMAGE_TAKEN}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding DamageTaken}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Defense.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationDtps, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DTPS}"
                                                   ToolTip="{Binding DataContext.DamageMeterBindings.TranslationDtpsTooltip, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DTPS_TOOLTIP}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding Dtps}"
                                                   ToolTip="{Binding DataContext.DamageMeterBindings.TranslationDtpsTooltip, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DTPS_TOOLTIP}"
                                                   Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                            <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationBiggestDamageTakenBySpellTopThree, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BIGGEST_DAMAGE_TAKEN_BY_SPELL_TOP_3}"
                                       Foreground="{StaticResource SolidColorBrush.Accent.Blue.1}"
                                       FontSize="12" FontWeight="Bold" Margin="2,8,2,3" />
                            <ItemsControl ItemsSource="{Binding TopDamageTakenBySpell}" ItemTemplate="{StaticResource YourStatsTopEntryTemplate}" />
                        </StackPanel>
                    </Border>
                    <Border Grid.Row="1" Grid.Column="1" Margin="5,5,0,2"
                            Style="{StaticResource YourStats.Combat.SectionCard}">
                        <StackPanel>
                            <Grid Margin="2,0,2,6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource YourStats.Combat.HeaderIcon}">
                                    <fa5:ImageAwesome Icon="Regular_Clock" Foreground="{StaticResource SolidColorBrush.Accent.Yellow.3}" />
                                </Border>
                                <TextBlock Grid.Column="1"
                                           Text="{Binding DataContext.DamageMeterBindings.TranslationYourStatsCombatSection, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=YOUR_STATS_COMBAT_SECTION}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Yellow.3}"
                                           Style="{StaticResource YourStats.SectionTitle}" />
                            </Grid>
                            <UniformGrid Columns="3">
                                <Border Style="{StaticResource YourStats.Combat.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationCombatTime, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=COMBAT_TIME}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding CombatTime}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Combat.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationFightCount, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=FIGHT_COUNT}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding FightCount}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource YourStats.Combat.MetricCard}">
                                    <StackPanel>
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationAverageFightDuration, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=AVERAGE_FIGHT_DURATION}"
                                                   Style="{StaticResource YourStats.MetricLabel}" />
                                        <TextBlock Text="{Binding AverageFightDuration}" Style="{StaticResource YourStats.MetricValue}" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>
        </DataTemplate>
        <DataTemplate x:Key="DamageStatsSnapshotTemplate" DataType="{x:Type damageMeter:DamageStatsSnapshot}">
            <ScrollViewer Margin="0,5,0,0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch">
                <StackPanel Margin="0,0,0,0">
                    <UniformGrid Columns="6">
                        <Border Style="{StaticResource TopStats.Damage.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Damage.HighlightIcon}">
                                    <iconPacks:PackIconGameIcons Kind="GooeyImpact" Width="24" Height="24" Margin="0,0,0,0" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationBiggestHit, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BIGGEST_HIT}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding BiggestHit.PlayerName}" Style="{StaticResource TopStats.HighlightPlayer}" />
                                    <TextBlock Text="{Binding BiggestHit.ValueString}" Style="{StaticResource TopStats.HighlightValue}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource TopStats.Damage.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Damage.HighlightIcon}">
                                    <iconPacks:PackIconGameIcons Kind="BulletImpacts" Width="24" Height="24" Margin="0,0,0,0" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPeakDpsFiveSeconds, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PEAK_DPS_5_SECONDS}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding PeakDpsFiveSeconds.PlayerName}" Style="{StaticResource TopStats.HighlightPlayer}" />
                                    <TextBlock Text="{Binding PeakDpsFiveSeconds.ValueString}" Style="{StaticResource TopStats.HighlightValue}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource TopStats.Damage.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Damage.HighlightIcon}">
                                    <iconPacks:PackIconGameIcons Kind="BulletImpacts" Width="24" Height="24" Margin="0,0,0,0" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationPeakDpsTenSeconds, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=PEAK_DPS_10_SECONDS}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding PeakDpsTenSeconds.PlayerName}" Style="{StaticResource TopStats.HighlightPlayer}" />
                                    <TextBlock Text="{Binding PeakDpsTenSeconds.ValueString}" Style="{StaticResource TopStats.HighlightValue}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource TopStats.Healing.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Healing.HighlightIcon}">
                                    <iconPacks:PackIconGameIcons Kind="Healing" Width="24" Height="24" Margin="0,0,0,0" Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTopHealer, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOP_HEALER}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Green.3}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding TopHealer.PlayerName}" Style="{StaticResource TopStats.HighlightPlayer}" />
                                    <TextBlock Text="{Binding TopHealer.ValueString}" Style="{StaticResource TopStats.HighlightValue}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource TopStats.Defense.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Defense.HighlightIcon}">
                                    <iconPacks:PackIconGameIcons Kind="SnakeBite" Width="24" Height="24" Margin="0,0,0,0" Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"/>
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTopDamageTaken, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOP_DAMAGE_TAKEN}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding MostDamageTaken.PlayerName}" Style="{StaticResource TopStats.HighlightPlayer}" />
                                    <TextBlock Text="{Binding MostDamageTaken.ValueString}" Style="{StaticResource TopStats.HighlightValue}" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Style="{StaticResource TopStats.Defense.HighlightCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource TopStats.Defense.HighlightIcon}">
                                    <fa5:ImageAwesome Icon="Solid_Users" Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                                </Border>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTotalTrackedFights, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL_TRACKED_FIGHTS}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"
                                               Style="{StaticResource TopStats.HighlightTitle}" />
                                    <TextBlock Text="{Binding TrackedFightCount}" Style="{StaticResource TopStats.HighlightValue}" />
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding TrackedFightDurationString}" Style="{StaticResource TopStats.HighlightPlayer}" Margin="0" />
                                        <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationTotalLabel, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL}"
                                                   Style="{StaticResource TopStats.HighlightPlayer}" Margin="4,0,0,0" />
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </UniformGrid>
                    <UniformGrid Columns="4">
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Solid_Bolt" Style="{StaticResource TopStats.RankingIcon}" Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationBiggestSingleHitTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BIGGEST_SINGLE_HIT_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopSingleHits}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Solid_ChartLine" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationBurstDamageFiveSecondsTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BURST_DAMAGE_5_SECONDS_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopBurstDamageFiveSeconds}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Regular_Clock" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationBurstDamageTenSecondsTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=BURST_DAMAGE_10_SECONDS_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopBurstDamageTenSeconds}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Solid_Bolt" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationTotalDamageTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOTAL_DAMAGE_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopTotalDamage}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Red.2}" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                    <UniformGrid Columns="4">
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Regular_Heart" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationEffectiveHealingTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=EFFECTIVE_HEALING_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Green.3}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopEffectiveHealing}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Solid_Plus" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationOverhealTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=OVERHEAL_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Green.3}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopOverheals}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Green.3}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <iconPacks:PackIconGameIcons Grid.Column="0" Grid.Row="0" Kind="SnakeBite" Style="{StaticResource TopStats.RankingGameIcon}"
                                                                 Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationDamageTakenTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DAMAGE_TAKEN_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopTakenDamage}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                            </StackPanel>
                        </Border>
                        <Border Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <fa5:ImageAwesome Grid.Column="0" Icon="Solid_Users" Style="{StaticResource TopStats.RankingIcon}"
                                                      Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationAttackedMobsPlayersTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=ATTACKED_MOBS_PLAYERS_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopAttackedTargets}" ItemTemplate="{StaticResource DamageStatsEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Blue.2}" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Border Grid.Column="0"
                                Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <iconPacks:PackIconGameIcons Grid.Column="0" Grid.Row="0" Kind="SkullCrack" Style="{StaticResource TopStats.RankingGameIcon}"
                                                                 Foreground="{StaticResource SolidColorBrush.Accent.Orange.3}"/>
                                    <TextBlock Grid.Column="1"
                                               Text="{Binding DataContext.DamageMeterBindings.TranslationTopMobKillContributionTop5, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=TOP_MOB_KILL_CONTRIBUTION_TOP_5}"
                                               Foreground="{StaticResource SolidColorBrush.Accent.Orange.3}"
                                               Style="{StaticResource TopStats.RankingTitle}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding TopMobKillContribution}"
                                              ItemTemplate="{StaticResource DamageStatsContributionEntryTemplate}"
                                              Foreground="{StaticResource SolidColorBrush.Accent.Orange.3}" />
                            </StackPanel>
                        </Border>
                        <Border Grid.Column="1" Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationDamageByType, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DAMAGE_BY_TYPE}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                           Margin="2,0,0,5"
                                           Style="{StaticResource TopStats.RankingTitle}" />
                                <ItemsControl ItemsSource="{Binding DamageTypeTotals}" ItemTemplate="{StaticResource DamageTypeStatsEntryTemplate}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <UniformGrid Columns="3" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                </ItemsControl>
                            </StackPanel>
                        </Border>
                        <Border Grid.Row="1"
                                Grid.Column="0"
                                Grid.ColumnSpan="2"
                                Style="{StaticResource DamageMeter.RankingCard}">
                            <StackPanel>
                                <TextBlock Text="{Binding DataContext.DamageMeterBindings.TranslationDamageByAbilityTop10, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, FallbackValue=DAMAGE_BY_ABILITY_TOP_10}"
                                           Foreground="{StaticResource SolidColorBrush.Accent.Red.2}"
                                           Margin="2,0,0,4"
                                           Style="{StaticResource TopStats.RankingTitle}" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <ContentControl Grid.Column="0"
                                                    Content="{Binding TopDamageSpellsLeftColumn}"
                                                    ContentTemplate="{StaticResource DamageSpellStatsColumnTemplate}"
                                                    HorizontalContentAlignment="Stretch" />
                                    <ContentControl Grid.Column="1"
                                                    Content="{Binding TopDamageSpellsRightColumn}"
                                                    ContentTemplate="{StaticResource DamageSpellStatsColumnTemplate}"
                                                    HorizontalContentAlignment="Stretch" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <TabControl Background="{StaticResource SolidColorBrush.Transparent}" BorderThickness="0">
            <TabItem Header="{Binding DamageMeterBindings.TranslationLive, FallbackValue=LIVE}">
                <TabControl Background="{StaticResource SolidColorBrush.Transparent}" BorderThickness="0">
                    <TabItem Header="{Binding DamageMeterBindings.TranslationMeter, FallbackValue=METER}">
                        <Grid Margin="3,7,3,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="8" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Style="{StaticResource App.TopFilterCard}" Margin="0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Style="{StaticResource App.FilterGroup}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Translation.Status, FallbackValue=STATUS}"
                                                       Style="{StaticResource App.FilterLabel}" />
                                            <Button MinWidth="190"
                                                    HorizontalAlignment="Stretch"
                                                    Style="{StaticResource Style.Button.Neutral}"
                                                    ToolTip="{Binding Translation.DamageMeterOnOff, FallbackValue=DAMAGE__METER__ON__OFF}"
                                                    Click="DamageMeterActivationToggle_Click">
                                                <StackPanel Orientation="Horizontal">
                                                    <fa5:ImageAwesome Width="16"
                                                                      Height="16"
                                                                      Margin="0,0,8,0"
                                                                      Icon="{Binding DamageMeterBindings.DamageMeterActivationToggleIcon, FallbackValue=Solid_ToggleOff}"
                                                                      Foreground="{Binding DamageMeterBindings.DamageMeterActivationToggleColor, FallbackValue={StaticResource SolidColorBrush.Text.1}}"
                                                                      IsHitTestVisible="False" />
                                                    <TextBlock VerticalAlignment="Center" Style="{StaticResource DamageMeter.TrackingStatusText}" />
                                                </StackPanel>
                                            </Button>
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="1" Style="{StaticResource App.FilterGroup}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Translation.Display, FallbackValue=DISPLAY}"
                                                       Style="{StaticResource App.FilterLabel}" />
                                            <ComboBox Width="210"
                                                      ItemsSource="{Binding DamageMeterBindings.DamageMeterSort}"
                                                      SelectedItem="{Binding DamageMeterBindings.DamageMeterSortSelection}"
                                                      IsSynchronizedWithCurrentItem="True"
                                                      DisplayMemberPath="Name"
                                                      SelectedValuePath="DamageMeterSortType" />
                                        </StackPanel>
                                    </Border>

                                    <Border Grid.Column="2" Style="{StaticResource App.FilterGroup}">
                                        <StackPanel>
                                            <TextBlock Text="{Binding DamageMeterBindings.TranslationContent, FallbackValue=CONTENT}"
                                                       Style="{StaticResource App.FilterLabel}" />
                                            <ComboBox Width="210"
                                                      ItemsSource="{Binding DamageMeterBindings.DamageMeterContentFilters}"
                                                      SelectedItem="{Binding DamageMeterBindings.DamageMeterContentFilterSelection, Mode=TwoWay}"
                                                      DisplayMemberPath="Name" />
                                        </StackPanel>
                                    </Border>

                                    <StackPanel Grid.Column="4"
                                                Orientation="Horizontal"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom">
                                        <Button Style="{StaticResource DamageMeter.ActionButton}"
                                                ToolTip="{Binding Translation.OpenExtendedDamageMeterWindow, FallbackValue=OPEN__EXTENDED__DAMAGE__METER__WINDOW}"
                                                Click="OpenDamageMeterWindow_Click">
                                            <fa5:ImageAwesome Icon="Solid_Expand" Style="{StaticResource DamageMeter.ActionIcon}" />
                                        </Button>
                                        <Button Style="{StaticResource DamageMeter.ActionButton}"
                                                ToolTip="{Binding Translation.CopyToClipboard, FallbackValue=COPY__TO__CLIPBOARD}"
                                                Click="CopyDamageMeterToClipboard_Click">
                                            <fa5:ImageAwesome Icon="Regular_Copy" Style="{StaticResource DamageMeter.ActionIcon}" />
                                        </Button>
                                        <Button Style="{StaticResource DamageMeter.ActionButton}"
                                                ToolTip="{Binding DamageMeterBindings.TranslationTakeASnapshotOfDamageMeterDescription, FallbackValue=TAKE_A_SNAPSHOT_OF_DAMAGE_METER}"
                                                Click="TakeSnapshot_Click">
                                            <fa5:ImageAwesome Icon="Solid_Camera" Style="{StaticResource DamageMeter.ActionIcon}" />
                                        </Button>
                                        <Button Style="{StaticResource DamageMeter.ActionButton}"
                                                ToolTip="{Binding Translation.DamageMeter, FallbackValue=DAMAGE__METER}"
                                                MouseEnter="OpenDamageMeterInfoPopup_MouseEnter"
                                                MouseLeave="CloseDamageMeterInfoPopup_MouseLeave">
                                            <fa5:ImageAwesome Icon="Solid_InfoCircle" Style="{StaticResource DamageMeter.ActionIcon}" />
                                        </Button>
                                        <Button Content="{Binding Translation.ResetDamageMeter, FallbackValue=RESET__DAMAGE__METER}"
                                                Margin="2,0,0,0"
                                                Style="{StaticResource DamageMeter.DangerButton}"
                                                Click="ResetDamageMeter_Click" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                            <ScrollViewer Grid.Row="2" Margin="-3,0,0,0"
                                          Style="{StaticResource DamageMeter.Live.PlayerList}">
                                <ItemsControl ItemsSource="{Binding DamageMeterBindings.DamageMeter, IsAsync=True, Mode=OneWay}"
                                              ItemTemplate="{StaticResource DamageMeterFragmentTemplate}" />
                            </ScrollViewer>
                            <ListBox Grid.Row="2" Margin="-3,0,0,0"
                                     Style="{StaticResource DamageMeter.Live.MobList}"
                                     ItemsSource="{Binding DamageMeterBindings.MobDamageMeter, IsAsync=True, Mode=OneWay}"
                                     ItemTemplate="{StaticResource MobDamageMeterFragmentTemplate}" />

                            <Grid Grid.Row="2"
                                  Panel.ZIndex="1"
                                  Visibility="{Binding IsDamageMeterPopupVisible, FallbackValue=Hidden}"
                                  Background="{StaticResource SolidColorBrush.Background.2}">
                                <StackPanel CanHorizontallyScroll="False"
                                            CanVerticallyScroll="True"
                                            Margin="14,12">
                                    <DockPanel Margin="0,0,0,16">
                                        <Grid Style="{StaticResource DamageMeter.DescriptionIcon}">
                                            <fa5:ImageAwesome Icon="Regular_DotCircle" Foreground="{StaticResource SolidColorBrush.Accent.Blue.3}" />
                                        </Grid>
                                        <TextBlock Text="{Binding Translation.DamageMeterDescription1}" Style="{StaticResource DamageMeter.DescriptionText}" />
                                    </DockPanel>
                                    <DockPanel Margin="0,0,0,16">
                                        <Grid Style="{StaticResource DamageMeter.DescriptionIcon}">
                                            <fa5:ImageAwesome Icon="Regular_DotCircle" Foreground="{StaticResource SolidColorBrush.Accent.Blue.3}" />
                                        </Grid>
                                        <TextBlock Text="{Binding Translation.DamageMeterDescription2}" Style="{StaticResource DamageMeter.DescriptionText}" />
                                    </DockPanel>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <TabItem Header="{Binding DamageMeterBindings.TranslationTopStats, FallbackValue=TOP_STATS}">
                        <ContentControl Content="{Binding DamageMeterBindings.CurrentDamageStats}" ContentTemplate="{StaticResource DamageStatsSnapshotTemplate}" />
                    </TabItem>
                    <TabItem Header="{Binding DamageMeterBindings.TranslationYourStats, FallbackValue=YOUR_STATS}">
                        <ContentControl Content="{Binding DamageMeterBindings.CurrentYourStats}" ContentTemplate="{StaticResource YourStatsSnapshotTemplate}" />
                    </TabItem>

                    <TabItem Header="{Binding Translation.Settings, FallbackValue=SETTINGS}">
                        <ScrollViewer Padding="10,10,12,0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" HorizontalContentAlignment="Stretch">
                            <Border Style="{StaticResource Settings.Card}">
                                <StackPanel>
                                    <Border Style="{StaticResource Settings.CardHeader}">
                                        <TextBlock Text="{Binding Translation.Settings, FallbackValue=SETTINGS}" Style="{StaticResource Settings.CardTitle}" />
                                    </Border>

                                    <Grid Margin="14,12,14,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="16" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <CheckBox IsChecked="{Binding DamageMeterBindings.IsDamageMeterResetByMapChangeActive}" Style="{StaticResource DamageMeter.SettingCheckBox}">
                                                <TextBlock Text="{Binding Translation.MapChangeReset, FallbackValue=MAP__CHANGE__RESET}" TextWrapping="Wrap" />
                                            </CheckBox>
                                            <CheckBox IsChecked="{Binding DamageMeterBindings.IsDamageMeterResetBeforeCombatActive}" Style="{StaticResource DamageMeter.SettingCheckBox}">
                                                <TextBlock Text="{Binding Translation.ResetBeforeCombat, FallbackValue=RESET__BEFORE__COMBAT}" TextWrapping="Wrap" />
                                            </CheckBox>
                                            <CheckBox IsChecked="{Binding DamageMeterBindings.IsSnapshotAfterMapChangeActive}" Style="{StaticResource DamageMeter.SettingCheckBox}">
                                                <TextBlock Text="{Binding Translation.SnapshotAfterMapChange, FallbackValue=SNAPSHOT__AFTER__MAP__CHANGE}" TextWrapping="Wrap" />
                                            </CheckBox>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2">
                                            <CheckBox IsChecked="{Binding DamageMeterBindings.ShortDamageMeterToClipboard}" Style="{StaticResource DamageMeter.SettingCheckBox}">
                                                <TextBlock Text="{Binding Translation.CopyShortDamageMeterToTheClipboard, FallbackValue=COPY__SHORT__DAMAGE__METER__TO__THE__CLIPBOARD}" TextWrapping="Wrap" />
                                            </CheckBox>
                                            <CheckBox IsChecked="{Binding DamageMeterBindings.OnlyDamageToPlayersCounts}" Style="{StaticResource DamageMeter.SettingCheckBox}">
                                                <TextBlock Text="{Binding Translation.OnlyDamageToPlayersCounts, FallbackValue=ONLY__DAMAGE__TO__PLAYERS__COUNTS}" TextWrapping="Wrap" />
                                            </CheckBox>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </ScrollViewer>
                    </TabItem>
                </TabControl>
            </TabItem>

            <TabItem Header="{Binding DamageMeterBindings.TranslationSnapshots, FallbackValue=SNAPSHOTS}">
                <Grid Margin="10,10,10,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="8" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            Style="{StaticResource App.TopFilterCard}"
                            Margin="0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Style="{StaticResource App.FilterGroup}">
                                <StackPanel>
                                    <TextBlock Text="{Binding DamageMeterBindings.TranslationSnapshots, FallbackValue=SNAPSHOTS}"
                                               Style="{StaticResource App.FilterLabel}" />
                                    <ComboBox Width="240"
                                              ItemsSource="{Binding DamageMeterBindings.DamageMeterSnapshots}"
                                              SelectedItem="{Binding DamageMeterBindings.DamageMeterSnapshotSelection}"
                                              IsSynchronizedWithCurrentItem="True"
                                              DisplayMemberPath="TimestampString"
                                              SelectedValuePath="DamageMeter" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource App.FilterGroup}" Margin="8,0,0,0">
                                <StackPanel>
                                    <TextBlock Text="{Binding DamageMeterBindings.TranslationContent, FallbackValue=CONTENT}"
                                               Style="{StaticResource App.FilterLabel}" />
                                    <ComboBox Width="210"
                                              ItemsSource="{Binding DamageMeterBindings.DamageMeterContentFilters}"
                                              SelectedItem="{Binding DamageMeterBindings.DamageMeterSnapshotContentFilterSelection, Mode=TwoWay}"
                                              DisplayMemberPath="Name" />
                                </StackPanel>
                            </Border>

                            <WrapPanel Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Bottom">
                                <Button Content="{Binding DamageMeterBindings.TranslationDeleteSelectedSnapshot, FallbackValue=DELETE__SELECTED__SNAPSHOT}"
                                        Margin="0,0,8,0"
                                        Style="{StaticResource DamageMeter.DangerButton}"
                                        Click="DeleteSelectedSnapshot_Click" />
                                <Button Content="{Binding DamageMeterBindings.TranslationDeleteAllSnapshots, FallbackValue=DELETE__ALL__SNAPSHOTS}"
                                        Margin="0,0,8,0"
                                        Style="{StaticResource DamageMeter.DangerButton}"
                                        Click="DeleteAllSnapshots_Click" />
                                <Button Style="{StaticResource DamageMeter.TextButton}"
                                        ToolTip="{Binding Translation.CopyToClipboard, FallbackValue=COPY__TO__CLIPBOARD}"
                                        Click="CopySnapshotToClipboard_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <fa5:ImageAwesome Icon="Regular_Copy"
                                                          Width="14"
                                                          Height="14"
                                                          Margin="0,0,8,0"
                                                          Style="{StaticResource Style.Button.Icon.White}" />
                                        <TextBlock Text="{Binding Translation.CopyToClipboard, FallbackValue=COPY__TO__CLIPBOARD}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </WrapPanel>
                        </Grid>
                    </Border>
                    <TabControl Grid.Row="2" Background="{StaticResource SolidColorBrush.Transparent}" BorderThickness="0">
                        <TabItem Header="{Binding DamageMeterBindings.TranslationMeter, FallbackValue=METER}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="8" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Style="{StaticResource App.TopFilterCard}" Margin="0,5,0,0">
                                    <Grid>
                                        <Border HorizontalAlignment="Left"
                                                Style="{StaticResource App.FilterGroup}">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Translation.Display, FallbackValue=DISPLAY}"
                                                           Style="{StaticResource App.FilterLabel}" />
                                                <ComboBox Width="210"
                                                          ItemsSource="{Binding DamageMeterBindings.DamageMeterSnapshotSort}"
                                                          SelectedItem="{Binding DamageMeterBindings.DamageMeterSnapshotSortSelection}"
                                                          IsSynchronizedWithCurrentItem="True"
                                                          DisplayMemberPath="Name"
                                                          SelectedValuePath="DamageMeterSortType" />
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>
                                <ScrollViewer Grid.Row="2" Margin="-3,0,0,0"
                                              Style="{StaticResource DamageMeter.Snapshot.PlayerList}">
                                    <ItemsControl ItemsSource="{Binding DamageMeterBindings.DamageMeterSnapshotSelection.DamageMeter, IsAsync=True, Mode=OneWay}"
                                                  ItemTemplate="{StaticResource DamageMeterSnapshotFragmentTemplate}" />
                                </ScrollViewer>
                                <ListBox Grid.Row="2" Margin="-3,0,0,0"
                                         Style="{StaticResource DamageMeter.Snapshot.MobList}"
                                         ItemsSource="{Binding DamageMeterBindings.DamageMeterSnapshotSelection.MobDamageMeter, IsAsync=True, Mode=OneWay}"
                                         ItemTemplate="{StaticResource MobDamageMeterFragmentTemplate}" />
                            </Grid>
                        </TabItem>
                        <TabItem Header="{Binding DamageMeterBindings.TranslationTopStats, FallbackValue=TOP_STATS}">
                            <ContentControl Content="{Binding DamageMeterBindings.SnapshotDamageStats}" ContentTemplate="{StaticResource DamageStatsSnapshotTemplate}" />
                        </TabItem>
                        <TabItem Header="{Binding DamageMeterBindings.TranslationYourStats, FallbackValue=YOUR_STATS}">
                            <ContentControl Content="{Binding DamageMeterBindings.SnapshotYourStats}" ContentTemplate="{StaticResource YourStatsSnapshotTemplate}" />
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
