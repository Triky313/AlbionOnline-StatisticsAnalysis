<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:models="clr-namespace:StatisticsAnalysisTool.Models"
                    mc:Ignorable="d">

    <DataTemplate x:Key="Donation" DataType="{x:Type models:Donation}">
        <StackPanel Orientation="Horizontal">
            <Image Style="{StaticResource Mail.Image.Style}" ToolTip="{Binding TranslationSilver}" />
            <TextBlock Text="{Binding Amount, StringFormat='{} {0:N0}'}" FontSize="20" FontWeight="Light" Width="150" 
                       Foreground="{StaticResource SolidColorBrush.Text.1}" VerticalAlignment="Center" />
            <TextBlock Text="{Binding Contributor}" Width="100" FontSize="20" 
                       Foreground="{StaticResource SolidColorBrush.Text.1}" VerticalAlignment="Center" />
        </StackPanel>
    </DataTemplate>

</ResourceDictionary>